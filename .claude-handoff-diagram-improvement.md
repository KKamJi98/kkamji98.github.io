# Claude Code Handoff: 블로그 다이어그램 품질 개선 + 스킬 고도화

## 작업 1: RAG 다이어그램 3개 수정 (최우선)

사용자가 지적한 문제:

### rag-05-secure-rag-flow.drawio
- Vector DB -> Retrieval이 이전엔 일자였는데 지금은 꺾임
- Retrieval -> Permission Filter 사이 "by role" label이 도형 간 여백 부족으로 가시성 해침
- **수정 방향**: Vector DB와 Retrieval을 같은 x축으로 정렬, 노드 간격 확대

### rag-06-latency-eval.drawio
- 점선 "hit bypass" -> Answer이 중간 도형과 겹침
- **수정 방향**: hit bypass edge를 위쪽으로 라우팅하거나 Answer 노드 위치 조정

### rag-07-rag-variants.drawio
- 전반적인 가시성 저하
- **수정 방향**: 레이아웃 단순화

## 작업 2: 전체 다이어그램 스킬 고도화 (리서치 기반)

agentskills.io + Archify 분석에서 도출된 개선 기회:

### HIGH 우선순위
- Eval 벤치마크 도입 (`evals/` 디렉토리): 스킬 품질 측정 피드백 루프

### MEDIUM 우선순위
- 컨텍스트 읽기 금지 목록: 첫 생성 전 불필요한 파일 읽기 차단
- 거짓 보고 방지 문장 SKILL.md 본문 강화
- Intent diff 스크립트: 두 intent 간 변경 추적 자동화

### LOW 우선순위
- 노드 수 상한 가이드라인
- SHA-256 해시 영수증

## 규칙 (반드시 준수)

1. **모든 cell에 `html=0`** (예외 없음)
2. **줄바꿈은 반드시 `&#xa;`** (`&#10;` 사용 금지 - Draw.io에서 문자 그대로 렌더됨)
3. **모든 edge `strokeWidth=1.25`**
4. **모든 cell `fontColor` 명시**
5. **Labeled edge는 `labelBackgroundColor=#FFFFFF`**
6. **export는 native drawio CLI 사용**: `xvfb-run -a drawio -x -f png -s 2 --crop -b 0 --no-sandbox`
7. **export 후 pad**: `uv run --with pillow python kkamji_scripts/blog/pad_diagram_margins.py --pad 110`
8. **HTML 마크업 금지**: `<font>`, `<b>`, `<br>` 태그 사용 불가
9. **의미 있는 edge label 삭제 금지**: 간격을 늘려서 해결
10. **`agent=` 속성 제거**

## 작업 환경

- Blog repo: `/home/kkamji/Code/kkamji98.github.io`
- Settings repo: `/home/kkamji/Code/kkamji-settings`
- Skill dir: `$HOME/.hermes/skills/drawio-generate-diagram`
- Export 도구: `/opt/drawio/drawio` 또는 `/usr/bin/drawio`
- Pad 도구: `kkamji_scripts/blog/pad_diagram_margins.py` (`--pad`, `--verify` 옵션)

## 검증 체크리스트

- [ ] `xmllint --noout` 통과
- [ ] `html=1` 잔존 0건
- [ ] `&#10;` 잔존 0건
- [ ] `strokeWidth=2` 잔존 0건
- [ ] `<font` 잔존 0건
- [ ] `agent=` 잔존 0건
- [ ] gitleaks 통과
- [ ] Jekyll build 통과
- [ ] OCR에서 핵심 label 읽힘
- [ ] Margin 균등 (L≈R≈T≈B)
